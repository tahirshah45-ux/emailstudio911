# ── SMTP configuration ─────────────────────────────────────────────
# SMTP_PROVIDER: gmail | microsoft | custom
SMTP_PROVIDER=gmail

# Credentials (for Gmail use an App Password: Google Account → Security → App Passwords)
SMTP_USER=you@example.com
SMTP_PASS=your-app-password

# Sender identity shown to the client
SMTP_FROM_NAME=911 Makers
SMTP_FROM_EMAIL=you@example.com

# Only required when SMTP_PROVIDER=custom
# SMTP_HOST=smtp.yourdomain.com
# SMTP_PORT=587
# SMTP_SECURE=false

# ── Storage ────────────────────────────────────────────────────────
# Directory where clients.json / emails.json are stored (default: ./data)
# DATA_DIR=./data
